{
  "name": "activity--cross-contract-calls",
  "version": "0.0.1",
  "description": "Learn to develop contracts for NEAR Protocol using AssemblyScript",
  "author": "Near Inc <hello@nearprotocol.com>",
  "license": "MIT",
  "scripts": {
    "build:contract:rust": "(cd contracts/rust && ./build_all.sh)",
    "build:contract:assemblyscript": "node asconfig.js",
    "test:contract": "asp --verbose",
    "build": "yarn build:contract:rust && yarn build:contract:assemblyscript",
    "test": "yarn test:contract"
  },
  "devDependencies": {
    "assemblyscript": "^0.9.4",
    "near-sdk-as": "^0.2.2-1",
    "near-shell": "^0.21.0",
    "near-vm": "^0.0.3"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}